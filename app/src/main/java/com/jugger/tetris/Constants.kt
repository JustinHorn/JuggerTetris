package com.jugger.tetris

import android.graphics.Color
import com.jugger.tetris.Basic.Brick

/**Enum of constants */
enum class Constants(val count: Int) {
    columns(8), rows(20), blockStartIndex(2), startSleepMilliSeconds(750), images(42);

}